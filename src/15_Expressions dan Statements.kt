// Expression dan Statements
// Expression = terdiri dari variable, operator dan sesuatu yg mengevaluasi ke dalam nilai tunggal.
// - if
// - when

fun sum(a: Int, b: Int) = a + b
fun main() = print(sum(1, 2 * 3))

// Statements = Inisialisasi sebuah variable